import { LoadImageDirective } from './load-image.directive';

describe('LoadImageDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadImageDirective();
    expect(directive).toBeTruthy();
  });
});
